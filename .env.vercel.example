# Vercel Environment Variables
# Copy these to your Vercel project settings

# Node Environment
NODE_ENV=production

# Application URL (set to your Vercel domain)
APP_URL=https://your-project.vercel.app

# Firebase Configuration (from Firebase Console)
# 1. Go to Firebase Console > Project Settings > Service Accounts
# 2. Click "Generate New Private Key"
# 3. Copy the values below from the JSON file

FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# Optional: Real-time Communication
# If using Pusher or Ably for WebSockets instead of Socket.IO
# PUSHER_APP_ID=your-pusher-app-id
# PUSHER_KEY=your-pusher-key
# PUSHER_SECRET=your-pusher-secret
# PUSHER_CLUSTER=your-pusher-cluster

# Optional: API Keys for external services
# OPENAI_API_KEY=sk-...
# SENDGRID_API_KEY=SG....

# Port (Vercel sets this automatically, but included for reference)
PORT=3000
